apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Values.name }}-svc
  name: {{ .Values.name }}-svc
  namespace: {{ .Values.namespace }}
spec:
  ports:
  - port: 3000
    protocol: TCP
    targetPort: 3000
    name: {{ .Values.name }}
  selector:
    app: {{ .Values.name }}
  type: {{ .Values.service.type }}